/**
 * Fetch Pleiades entries for every pleiades:* id referenced in acquisitions.json,
 * write them to data/gazetteers/pleiades.json.
 *
 * Stub — wire to Pleiades JSON dumps or the per-place JSON endpoints once the
 * first real export is ingested and we know which IDs we actually need.
 */
console.error('refresh-gazetteer: not yet implemented.');
process.exit(0);
